# 第一章 二层技术

### 堆叠

#### 在逻辑上虚拟为一台设备，对外统一管理，从控制平面来说是一台设备，从而提高可靠性，简化管理
#### 优势

##### 提高可靠性
##### 简化网络管理
##### 提高冗余可靠性
##### 优化流量转发和负载分担
#### 劣势

##### CPU利用率变低，有一台不工作
##### 设备升级麻烦，会造成业务中断
##### 设备兼容性，需要同厂商版本
### M-LAG

#### 跨设备链路聚合
#### 优势

##### 提供设备级冗余 避免单点故障
##### 支持不同版本不同型号，独立升级不会业务中断
##### 数据转发在不同设备间分部，提高性能和可靠性
##### 分布式 更稳定
### RRPP快速环网保护协议

#### 一种用于二层环网的快速保护协议

##### 城域网和企业网
#### 适合环形拓扑

##### 通过阻塞一条链路来消除环路，当链路发生故障时快速打开阻塞链路，实现毫秒级收敛
### 交换机特性

#### hybrid接口

##### 用于需要多个vlan且对不同vlan进行不通打标签处理的场景
##### 可以承载多个vlan，并且能够对不同VLAN数据灵活设置是否打tag/untag的接口类型
##### 常用接AP/IP电话
#### access接口

##### 只支持1个vlan

###### 不打tag
##### 接PC 
#### trunk接口

##### 支持多个vlan

###### 默认都打tag
##### 交换机互联
#### QinQ接口

##### vlan套vlan（双标签）
##### 支持双标签封装的接口，用于对数据进行外层vlan封装
##### 主要用于运营商网络
##### 可以支持4094*4094个vlan
### QinQ

#### 作用

##### 打上双层标签，使私网的vlan透传到公网，外层放的是公网的标签，内层放的是私网的标签，在公网上剥离公网标签，两边设备实现公网互访
#### 实现

##### 1.基本QinQ

###### 对进入二层QinQ接口的所有帧打上相同的外层tag

####### 不灵活
##### 2.灵活QinQ

###### 根据条件（vlan、端口等）选择性添加不同的外层tag
#### 广泛用于运营商、多租户、多用户隔离场景
### 二层设备收到一个数据帧如何处理？

#### 已知单播

##### 单播转发
#### 未知单播

##### 除了入接口外都泛洪
#### 未知广播

##### 除了入接口外都泛洪
#### 未知组播

##### 除了入接口外都泛洪/IGMP控制
### 三层网络设备对无路由数据包处理方式

#### 无路由直接丢包
### 未知单播什么时候会出现

#### 绑定了ARP，但是没有这台MAC，或者STP拓扑变更导致MAC表刷新
### 以太网帧分为几种？

#### ethernet2

##### IP网络中传输的业务数据（IP ARP ICMP）
#####  目的MAC 源MAC Type 数据 FCS
#### 802.3+LLC

##### STP协议报文以及ISIS发送的报文
##### 目的MAC 源MAC 长度 LLC头部 SNAP头部 数据 FCS
#### 802.3+LLC+SNAP

##### 在802.3基础上补一个类似Type的机制
##### SNAP是在LLC基础上扩展，通过OUI+PID标识协议
#### vlan 802.1q

##### 在以太网帧中插入4字节的VLAN TAG
#### QinQ 802.1ad
### 交换机如何判断数据帧属于什么播类型？

#### 通过目的MAC地址第一个字节的第7bit
#### 0

##### 单播
#### 1

##### 组播
#### 目的MAC全F

##### 广播
### 数据链路层上层用什么数字去表面承载的是IPv4还是v6

#### 以太网帧头部中的type类型
#### 0x0800

##### Ipv4
#### 0x86DD

##### ipv6
### MAC地址漂移的原因有哪些？怎么避免？

#### 原理

##### 同一MAC地址从不同端口被学习，通常由环路、链路不一致或虚拟化引起
#### 原因

##### 二层环路导致的MAC表震荡
##### 广播风暴
##### 攻击行为

###### MAC flood 伪造mac
##### 链路抖动 网络拓扑频繁变化
##### 主备链路 主备切换时也会MAC地址表漂移
#### 解决方法

##### 1.MAC地址漂移检测

###### 提高某一接口MAC地址学习优先级

####### MAC-learing priority 

######## 默认0，范围0-3,越大越优
###### 不允许相同优先级的接口发生MAC地址表项覆盖

####### undo mac-learing priority allow-flap平、
##### 2.配置全局MAC地址漂移检测告警
##### 3.STP消除二层环路
##### 4.对关键设备进行静态MAC绑定
### 交换机MAC地址学习的两种模式

#### IVL独享模式

##### vlan内学习到的mac地址为vlan内部，并不会共享给其他vlan 
##### 默认
#### SVL共享模式

##### vlan内学习到mac地址，会共享给所有vlan
### vlan的类型

#### 基于端口vlan
#### 基于MAC地址vlan
#### 基于协议vlan
#### 基于IP子网VLAN
#### 除了基于端口vlan，其他vlan类型都需要所有支持的接口类型都为hybrid，放行所有vlan
#### 默认优先级： MAC vlan> IP 子网> 协议 vlan>端口vlan
### 三层交换机数据转发原理

#### 同一vlan内通信L2转发

##### 通过MAC地址表二层转发
#### 不同vlan间通信L3转发

##### 先查路由表确定下一条，再差ARP表获取MAC，最后封装成新的二层帧转发
#### 二层转发同vlan

##### 1.主机A->主机B
##### 2.A通过ARP获取B的MAC
##### 3.交换机 查MAC表 直接转发
#### 三层转发跨vlan

##### 1.PC1主机发包，发现目标IP不在本网段，发给网关
##### 2.三层交换机收到帧 ：目的MAC=网关MAC，触发三层处理
##### 3.查路由表 查找：目的IP 的下一跳
##### 4.查ARP表 下一跳IP对应的MAC地址
##### 5.重封装二层帧

###### 交换机将

####### 源MAC->出接口MAC
####### 目的MAC->下一跳MAC
####### VLAN->目标vlan
##### 后续核心优化

###### 交换机会建立FIB（转发表），ARP邻接表
###### 后续数据包直接走硬件ASIC转发，不再走CPU
###### 首次建立转发表项，后续通过硬件实现转发，加快转发速度
###### 正确命中转发表->决定是否走CPU
### MVRP模式

#### MVRP是用于再二层网络中动态分发vlan信息的协议
#### normal模式

##### 可传递和动态学习所有的vlan
#### fixed模式

##### 只传递vlan1和静态vlan，不传递动态vlan
#### forbidden模式

##### 只传递vlan1，不传递静态vlan，也不学习动态vlan
#### MVRP运行的实体是端口，要求端口是trunk并放行所有vlan
### MRU和MTU区别

#### 最大接受单元

##### 接受上限
#### 最大传输单元

##### 发送上限
### 协商MTU目的

#### 为了保证双方报文大小一致，避免分片和丢包
#### 实际场景PPPoE 头部 6字节PPP头部长度 2字节二层数据帧头 ，有效载荷1500字节，则MTU=1492
### 加密算法和认证算法

#### 加密算法

##### 用于对数据进行加密，使未授权用户无法读取数据内容，保证安全传输

###### 对称

####### DES
####### 3DES
####### AES
###### 非对称

####### RSA
#### 认证算法

##### 用于验证数据来源和完整性，确认通信双方身份

###### SHA
###### MD5
###### HMAC

####### 哈希+密钥
### PPP（不考）

#### PPP链路建立过程

##### 点对点链路层协议
##### 整体流程

###### 链路建立->LCP->认证->NCP->数据传输
##### 三个协议族

###### 链路控制协议族LCP

####### 建立链路
###### 网络层控制协议族NCP

####### 完成网络层协商
###### 认证协议族CHAP和PAP

####### 认证保证安全
##### 五个阶段

###### Dead
###### Estabilish

####### LCP协商阶段

######## 作用

######### 建立、配置、参数协商、测试链路
######## 协商参数

######### MRU最大接受单元
######### 认证方式PAP / CHAP
######### Magic Number 检测环路
######## 过程 

######### 发送Configure -request
######### 对端回复

########## ack同意
########## Nak 建议修改
########## Reject 拒绝
###### Authenticate

####### 认证阶段

######## 缺省不认证
######## PAP 明文 两次交互
######## CHAP 三次握手 MD5加密 周期性验证
###### Network

####### NCP阶段

######## NCP协商
######## NCP负责为上层协议分配网络层参数
###### Terminate
#### PPP中的LCP有哪些功能

##### 链路建立和拆除
##### 参数协商

###### MRU
###### 压缩方式
###### 认证协议
##### 认证方式协商

###### PAP/CHAP
##### MLP协商（多链路）
#### LCP协商过程

##### 链路两端交互config-request同队对方链路层参数则回ack，不同意就nak or reject
##### 如果没有收到对端ACK or NAK，3s重传，连续重传10次没收到，认为对方不可用，停止发送config-request
##### LCP协商参数

###### MRU 

####### 最大接受单元
###### Authentication-Protocal

####### 认证方式
###### Magic-number

####### 检测环路和不正常的连接错误
###### Quality-protocol

####### 是否要使用链路质量监控
###### Protocol-field-compression

####### 是否启用protocol字段压缩 16bit-6bit
###### ACFC

####### 是否要压缩地址和控制字段
#### LCP如何检测环路

##### 魔术字
##### 魔术字工作机制

###### 1.每个设备启动PPP生成一个随机的魔术字
###### 2.在LCP报文中携带魔术字
###### 3.对端收到后

####### 正常情况下 魔术字不同
####### 如果收到带相同魔术字的LCP报文，则认为链路中存在环路
###### 若出现环路，认为链路异常，拒绝连接或断开PPP链路
#### NCP都协商什么内容

##### NCP是协议族
##### 网络层协议的协商 IPCP IPv6CP (control protocol)
##### MPLS IPX OSICP （上层运行ISIS时）
#### IPCP的动态和静态协商场景

##### 静态

###### 两台路由器设备，静态配置IP地址的PPP链路协商
##### 动态

###### PPPoE拨号
#### PAP/CHAP认证过程

##### PAP

###### 两次握手 明文
###### 客户端发送用户名和密码 明文
###### 服务器校验后返回认证结果
##### CHAP

###### 三次握手 加密 周期性发送认证
###### 服务器发送Challenge数据包 随机数

####### 报文ID 随机数 用户名
###### 客户端用密码 + 随机数计算MD5值并返回
###### 服务器用相同算法计算并比对结果
### PPPoE协商过程

#### 1.发现阶段

##### 1.Client广播一个PADI PPPoE Active Discovery Initation报文 （PADI）

###### 目的MAC地址为广播地址
##### 2.Server发送一个PPPoE  Offer报文（PADO）给client

###### 目的MAC地址为Client的单播地址
##### 3.Client选择最先受到的PADO报文对应的服务器发送一个单播PADR Request报文给Server

###### 单播
##### 4.Server发送单播PADS Session-confirm报文给Client 会话建立完成

###### 单播
#### 2.会话阶段

##### PPP的建立流程

###### LCP->认证->NCP->数据传输
#### 任何会话要被终止时回发送PADT Terminate报文，Client Server都可以发PADT
### Smart-link

#### 一种链路保护技术，通过主备链路机制实现快速切换
#### 配一条主链路Master

##### 配一条备链路Slave
#### 正常时走主链路，故障时快速切换到备链路
#### STP动态收敛阻塞 ，Smart link静态主备阻塞一条链路
### Smart-link实现负载分担方式

#### 多实例Smart-link+ 绑定不同vlan分流
#### 不同vlan走不同主链路
### Smart link链路故障处理

#### 主链路故障，备链路切为Forwarding
#### 在新的主链路通过MAC Flush报文通知设备更新MAC ARP表项

##### 组播
#### 上游设备要在 控制VLAN列表中收到Flush报文才会处理Flush报文，否则直接转发
#### 故障恢复处理

##### 主链路恢复后 默认不抢占，在备链路配置Smart LINK组回切，回切计时器超时，备用链路被阻塞且切换到待命状态，主链路切换到转发状态

###### Smart link 组回切
##### 也可以手动回切

###### 强制回切到主链路
### private vlan 原理及应用

#### 私有vlan 是在同一个vlan内实现二层隔离的技术
#### 通过主vlan和从vlan的划分，实现控制不同主机之间的通信
#### 三种vlan类型

##### 主vlan primary vlan

###### 对外通信的vlan
##### 从vlan Secondary vlan

###### Isolated 隔离vlan

####### 主机之间不能互通
###### Community 互通vlan

####### 同组内可以通信 不同组隔离
#### 应用场景

##### 运营商接入

###### 用户之间隔离，防止互相攻击
##### 数据中心服务器

###### 多租户环境 VM隔离
##### 校园网/企业网接入

###### 用户互相隔离 只访问网关
### 本地代理ARP和普通代理ARP

#### 普通代理ARP

##### 当主机访问其他网段时，由网关代替目标主机响应ARP请求，从而实现跨网段通信

###### 跨网段
##### 目的

###### 实现通信
#### 本地代理ARP

##### 在同一网段内，网关代替目标主机响应ARP请求，实现主机间的二层隔离；比如说private vlan或者sub vlan场景

###### 同网段
##### 同一网段 拿不到目标主机的MAC地址，无法直接二层通信，强制走网关，通过网关三层转发
##### 目的

###### 实现隔离
### vlan聚合 super vlan

#### super vlan 将多个子vlan共享同一个三层网关

##### 集中ARP表，实现跨VLAN的三层转发
#### 一个super vlan可以包含一个或多个不同广播域的sub vlan
#### 作用

##### 减少地址浪费
### super vlan如何实现vlan间通信

#### super vlan 通过三层网关+ARP代理机制，实现不同子vlan之间通信
#### 所有子vlan之间通信都被“强制三层化”，找三层网关，通过网关转发给其他子vlan
